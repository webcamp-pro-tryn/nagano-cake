<div class ="container">
  <div class="row">
    <div class ="col-xs-12">
  <h2 class="head-line title_h2">会員の方はこちらからログイン</h2>
    </div>

<div class="host-login col-md-6 offset-md-3">
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
<table border="0" align="center" >
  <tr height="50">
    <th>メールアドレス　　</th>
    <td>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", size:50 %>
    </td>
  </tr>
  <tr height="50">
    <th>パスワード </th>
    <td><%= f.password_field :password, autocomplete: "current-password", size:50 %>
    </td>
  </tr>
</table>
<h6>
  <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
  <%= link_to "=>パスワードを忘れた方はこちら", new_password_path(resource_name) %>
  <% end %>
</h6>
  <div class="actions">
    <%= f.submit "ログイン",class:"btn btn-primary w-15" %>
  </div>
  <% end %>
</div>
</div>
</div>

<div class = "sign-link">
 <h4 class= "head-line">
 登録がお済みでない方は</h4>
<%= link_to "こちら", new_registration_path(resource_name) %>から新規登録を行ってください
</div>
 <!--  <%# if devise_mapping.rememberable? %>
    <div class="field">
      <%#= f.check_box :remember_me %>
      <%#= f.label :remember_me %>
    </div>
  <%# end %> -->
